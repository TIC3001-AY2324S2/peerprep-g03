export type QuestionsField = {
    id: string;
    title: string;
    description: string;
    categories: string;
    complexity: string;
  };

export type MatchesField = {
    topic: string;
    difficulty: string;
  };

  export type CategoriesField = {
    value: string;
    label: string;
  };